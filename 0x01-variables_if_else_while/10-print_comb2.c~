#include <stdio.h>

/**
 * main- void
 * description: Print numbers 00 to 99
 * Return: 0
 */

int main(void)
int a,b;
a = 0
b = 0
	for (a <= 9 ; a++)
	{
	for (b <= 9 ; b++)
	{
		putchar(a)
		putchar(b)
	}
	putchar(',')
	putchar(' ')

return (0);
}

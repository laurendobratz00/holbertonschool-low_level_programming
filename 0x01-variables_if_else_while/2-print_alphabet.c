/**
 * main- void
 * description: Print a-z
 * Return: 0
 */

#include <stdio.h>

int main(void)
{
	char a;
	char x = '\n';

	for (a = 'a' ; a <= 'z' ; a++)
{
	putchar(a);
	}
	putchar(x);
		return (0);
	}

#include <stdio.h>
#include "holberton.h"

/**
 * main- void
 * description: print alphabet 10 times
 * Return: 0
 */

void print_alphabet(void);

{
	char a;
	char x = '\n';
	for(a = 'a' ; a <= 'z' ; a++)
	{putchar(a);
	}
	putchar(x);
	return (0);
}

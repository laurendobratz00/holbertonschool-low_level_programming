#include <stdio.h>
#include "holberton.h"
#include <ctype.h>

/**
 * main- void
 * description: check for lowercase
 * Return: 0
 */

int _islower(int c)
{
	if(c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
		return (0);
}

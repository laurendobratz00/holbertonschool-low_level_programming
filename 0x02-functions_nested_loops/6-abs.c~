#include <stdio.h>
#include "holberton.h"
#include <stdlib.h>

/*
 * _abs(int) - printing absolute values
 * description: printing absolute values
 * @n:
 * Return: 0
 */

int _abs(int);
{
	int x = abs(n);

	if (n >= 0)
	{
		_putchar(n);
	}
	else
	return (0);
}

#include "holberton.h"

/**
 * _strlen - return the length of a string
 * description: return the length of a string
 * Return: 0
 */

int _strlen(char *s)
{
	int len;
	char s;

	while (s[len] != '\0')
		return (len);
}
